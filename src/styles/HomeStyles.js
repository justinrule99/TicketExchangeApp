import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
   mainView: {
       alignItems: 'center',
       width: '100%',
       height: '100%',
       backgroundColor: '#F3C65F'
   },

    headerContainer: {
        alignItems: 'center',
        marginTop: 100,
        marginBottom: 70
    },

    buttonContainer: {
        width: '75%'
    }
});